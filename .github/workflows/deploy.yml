name: Deploy

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and run
        env:
          DEPLOY_PORT: ${{ vars.DEPLOY_PORT || '80' }}
          IMAGE_NAME: better-digital-clock
          CONTAINER_NAME: better-digital-clock
        run: |
          docker build -t $IMAGE_NAME .
          docker stop $CONTAINER_NAME 2>/dev/null || true
          docker rm $CONTAINER_NAME 2>/dev/null || true
          docker run -d --name $CONTAINER_NAME -p ${DEPLOY_PORT}:80 --restart unless-stopped $IMAGE_NAME
          echo "Deployed container $CONTAINER_NAME on port $DEPLOY_PORT"
